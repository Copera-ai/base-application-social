diff --git a/cjs/index.js b/cjs/index.js
index 7ab3b6c19a8862eece7cf08d3ceec5dbea0fc1f0..c9052fe0578a95c07d2a7d1d8dbe72fe4e4b2d8c 100644
--- a/cjs/index.js
+++ b/cjs/index.js
@@ -117,7 +117,7 @@ for (let i = 0; i < flen; i += 1) {
     const newText = replaceAlias(text, file);
     if (text !== newText) {
         changedFileCount += 1;
-        console.log(`${file}: replaced ${replaceCount - prevReplaceCount} paths`);
+        // console.log(`${file}: replaced ${replaceCount - prevReplaceCount} paths`);
         fs_1.writeFileSync(file, newText, 'utf8');
     }
 }
diff --git a/lib/index.js b/lib/index.js
index 10b82dbed1705ac971ebc267d90e0afd5590bf12..9f0555193d3003ab537aa0d6a1fda131304913f7 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -115,7 +115,7 @@ for (let i = 0; i < flen; i += 1) {
     const newText = replaceAlias(text, file);
     if (text !== newText) {
         changedFileCount += 1;
-        console.log(`${file}: replaced ${replaceCount - prevReplaceCount} paths`);
+        // console.log(`${file}: replaced ${replaceCount - prevReplaceCount} paths`);
         writeFileSync(file, newText, 'utf8');
     }
 }
